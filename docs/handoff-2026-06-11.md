# 交接文件 — 2026-06-11（音高 benchmark + 顯示系統兩大輪完成時點）

> 給下一個 session / 協作者的狀態快照。架構性內容看耐久文檔：
> [pitch-benchmark.md](pitch-benchmark.md)（gold/評測/轉譜鏈）、
> [display-grid.md](display-grid.md)（顯示系統）、MEMORY.md（決策史，內部私有）。

## 一頁總覽

**轉譜品質**（chidori，vs 全驗證 humangold）：
note-level `52.6% → 72.8%`（冠軍 = GAME extract + classic fallback 的 union）；
frame exact `.600 → .728`；原始 YT guide 信用跌至 `.766`。
byoushin 跨歌驗證通過（生產鏈 `.382 → .529`，要一個 snakemake target 就有）。

**顯示系統**（v10 — 兩輪 Kojek 驗收回饋後）：固定四分音符寬度、固定 mora/換句
間隙（0.125 拍）、固定頁寬（16 拍、1–3 句/頁）、碎音/抖音清理、時間 warp 同步。
v9 輪修：sprite PADDING 契約（間隙被吃掉的根因）、歌詞行邊界優先切分
（酔った振り/吹く青風不再腰斬）、長休息 flip/park/倒數四拍、mid2csv BPM 2dp 對齊。
v10 輪修：間隙 0.25→0.125 拍（太疏）、短句間 quick-flip（下一句音高預覽 ≈ 75% gap）、
`--pitch-patch` 人耳顯示層修正（52.4s た Eb4→Eb3；鏡像句 140.3 本來就對 = 證據）。
另修 chidori 假名（私=わたし、青風=あおかぜ、東風=こち、海=うみ；
`overrides/chidori.json` + Snakefile tokenize 自動接 override）。
成品：`outputs/chidori/karaoke...gameunion_v10grid.mp4`（**Kojek 已驗收通過**）。
v10 後追加：mora 間隙再減半 0.125→0.0625 拍（Kojek 嫌 0.125 仍疏）；
**byoushin 全自動 v10 渲染**（`outputs/byoushinwo-kamu/karaoke_gamescore_v10grid.mp4`，
零人耳輸入、零 per-song 調參，display 泛化測試 — 等耳測）。
chidori canonical grid 已同步 0.0625（mp4 未重渲，併入 v11 恆速 slot 輪）。

## ⚠ 未 commit 清單（display 輪全部只在 working tree！）

branch `codex/render-ruby-marker-fixes`，上次 commit 到 GAME 鏈為止（233eefc）。
之後的 display 整輪 + metrics 都未入庫：

| 類別 | 檔案 |
|---|---|
| 新腳本 | `scripts/make_display_grid.py`（核心）、`make_flat_bar_skin.py`、`eval_note_metrics.py` |
| 修改 | `render_mp4.py`（--time-warp）、`run_game_chain.py`（grid 鏈）、`melody_union.py`（quantize/gap/監控）、`score_note_postfix.py`（--keep-repeats、tail-allowance 系）、`src/karaoke_jp/midi_markers.py`（tail allowance + pack 模式）、`add_midi_markers.py`（pack/tail CLI） |
| 新資源 | `render_assets/`（assets_flat.json、settings_wide.json）、`docs/display-grid.md`、本檔 |
| 文檔 | `MEMORY.md`、`docs/pitch-benchmark.md` |
| v9 輪追加 | `tests/test_display_grid.py`（11 tests）、`overrides/chidori.json`、`Snakefile`（tokenize override 接線）、chidori 全部 LRC/aligned/tokens 假名修正（outputs/ 未追蹤） |
| 不在範圍 | `.claude/`、`data/`、`experiments/`、`songs/{aina-kakumei,byoushinwo-kamu,kuzurinen}`（多 session 來源，待 Kojek 裁決）|

建議 commit 切法：`display: standardized bar grid + time warp` /
`metrics: MIREX note metrics` / `docs: handoff + display doc`。

## 驗證狀態

- 測試：**121 passed**（v9 補上 grid/warp/skin 單元測試：行邊界切分、打包、
  間隙不重疊、warp 單調、count-in 錨點、sprite PADDING 契約）
- chidori 渲染 QA：48s（重疊/碎音消失）、59s（三連音+量化寬度）、81s（同寬）
  通過；frames 在 `tmp/chidori-humanfix-qa/v8_qa.png`；v9 QA 幀同目錄
- benchmark 數字未受 display 輪影響（grid 是顯示專用，eval 候選不變）

## 待辦（按優先序）

1. **Kojek 驗收 v10grid render** → 通過則把 chidori 顯示 canonical 切到
   GAME+grid 鏈，並考慮 byoushin 同款渲染
2. **Commit v9/v10 輪**（v8 輪已入庫 c4a2231/e0cceab/9de9b4e）
3. **✅ v11 顯示輪已實作（2026-06-11）**：
   (a) 恆速 slot：slot = 量化時值本身、bar 畫 slot − gap（間隙吃音符尾巴，
   同樂譜排版）— on-grid 演唱游標恆速，變速本身成為搶/拖拍訊號；
   (b) 換氣間隙：真實靜默 >0.25s → slot 間插 0.5q 寬縫（`--breath-gap` /
   `--breath-units`），游標在真實換氣時掃過。`phrase_width()` 與 layout
   鏡像（有測試把關）。成品：chidori `...gameunion_v11grid.mp4`、
   byoushin `karaoke_gamescore_v11grid.mp4`（用 boundary-fixed canonical LRC）
   — **等 Kojek 驗收**。128 passed。
4. **句首句尾自動修復 → canonical**（Kojek byoushin 耳測要求：句首句尾必須自動對準、
   可泛化；句中不強求）。已從 alignment ablation 全量結果找到**跨三首 gold 的單一配置**：
   `midi_timing --first-mora-min-delay 0.05 --first-mora-gate-prev-gap 0.75
   --first-mora-gate-lead-tolerance 0.08 --absorb-trailing-notes`
   ＋ `line_end_repair --tail-top-db 26 --next-guard 0.25 --tail-gap 0.12`
   → 三首歌句首 MAE 0.106–0.176s／句尾 0.131–0.133s（canonical 裸 mora_default：
   0.18–0.25／0.16–0.27）。byoushin sidecar 實套：8/47 句首（原搶跑 +0.5~1.5s）、
   18/47 句尾（延音 +0.2~2.2s）被修，`karaoke_gamescore_v10grid_bndfix.mp4` 等耳測。
   **✅ 已升 canonical**（2026-06-11 耳測通過後）：Snakefile `midi_timing` 規則帶
   gate flags、新 `line_end_repair` 規則插在 export_lrc 前
   （midi_timing → aligned_midi.raw.json → line_end_repair → aligned_midi.json）。
   byoushin 經 Snakemake 重出的 karaoke.lrc 與耳測版逐 byte 一致。
   既有歌曲產物不會被 mtime trigger 意外重建；要套新鏈用 `-f` 強制。
   已知弱點：chidori 句首 within-250ms 僅 60%（p90 0.43s）；
   上游 ASR 變體選擇仍是 per-song 風險（chidori 要 vad_short，壞 ASR 後處理救不了）。
   **Kojek bndfix 耳測（2026-06-11）：「挺好」，嚴重失誤僅 2 句**，皆為
   行邊界 note-ownership 家族（= memory 早已點名的句中對齊下一步）：
   (a) 末句 ハレタ レイラ@240.6 — 字被分到前句「いらないから」延音尾
   （240.56-241.65），本人的音在 242.9+（氣聲、轉譜偏散）→ 提早 ~2.4s；
   (b) 3:08 よ ad-lib ×3（188.4/189.25/189.46 三顆，歌詞只寫一個）→ 下一句
   このまま 首 mora 被多餘的 よ 拉早（gate 已推 +1.0s 仍不夠，人耳仍判偷跑）。
   兩案 = ownership 修復輪的具體測例（ad-lib 重複偵測 + 前句延音尾不可外借）。
5. 第三首歌 gold（`songs/aina-kakumei-piano/` 已備，OMR 進行中 — aina-kakumei 即第三首）
4. 殘餘人耳項：B 家族抽查 29.9–31.6s、低音 Eb3@52.0
   （52.0 那顆 = v9 驗收時 53s「た」低音缺席的根因：union 給了 Eb4）
5. 歌唱-onset 版 gold（讓 MIREX note metrics 可比文獻）
6. 更遠：GAME fine-tune（屆時才需要資料集 survey）

## ⚠ 方向修正輪(2026-06-11 晚,教授回饋)

教授判定 survey 不到位(Fu&Su ISMIR'19、Wang&Jang CTC/CE 都沒看)、
不該 pitch-level 起手、note 切分該考慮 CTC、公開 benchmark 沒跑。
**完整補課 + benchmark 計畫已成文:[ast-survey.md](ast-survey.md)**(三個
research agent 調查,全部一手來源)。要點:
- AST SOTA 座標系:MIR-ST500 COnPOff — baseline 45.8 / CE+CTC 57.4 /
  SOTA Mel-RoFormer 62.5;我們的 GAME 系**從無已發表基準數字** → 跑了就是貢獻
- 日文雙空白:複音 AST 與歌詞對齊都沒有公開日文 benchmark → 我們的 gold
  可轉化為首個日文 mora 級基準
- CTC 共識:「定位符號不定位邊界」— 要 tatum-CTC / CE+CTC / label-priors 配方,
  不是裸 CTC;對齊側換 CTC forced alignment(SOFA 日文 ckpt / MMS 卡拉OK微調)
- 下一步優先序改寫:benchmark(MIR-ST500 + Kiritan)> 對齊換骨 > 其餘待辦

## 環境備忘

- 5 個 venv；GAME 專用 `~/venvs/karaoke-jp-game`（**RTX 5090 必須 torch cu129**）
- flat bar skin 是生成物：新機器先跑 `scripts/make_flat_bar_skin.py`
- Snakemake 永遠帶 `--rerun-triggers mtime`；opt-in targets：
  `melody_markers.scorefix.mid`（classic 鏈）、`melody_markers.gamescore.mid`
  （GAME 鏈，現在輸出 grid MIDI + `.warp.json`，render 需 `--time-warp`）

## 本兩輪最重要的教訓（詳見 MEMORY.md）

1. 不對 YT guide 調參；「guide 與 F0 tracker 一致」≠ 獨立證據（同被唱腔帶偏）
2. 顯示異常先查 settings.json 覆蓋值（BAR_AREA_WIDTH=1000 事件）
3. render_mp4 路徑選項一律 resolve-before-chdir（踩兩次）
4. 改檔禁 `s.index` 切片，一律 assert count==1（踩兩次）
5. 負結果照價記錄：chroma snap、Viterbi v1、GAME on mix、align 當主源、
   sprite 內縮間隙 — 全部實測否決，不要走回頭路
