name: karaoke-jp-render
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - pip:
      - Pillow>=10.0
      - ffmpeg-python>=0.2.0
      # MID2BAR-Player fork installed from third_party/ at M4.
